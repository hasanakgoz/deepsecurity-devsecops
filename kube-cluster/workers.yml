---
- hosts: tag_role_kube_masternode
  become: yes
  gather_facts: false
  environment:
    masternode_ip: "{{ play_hosts }}"
  tasks:
    - name: get join command
      shell: kubeadm token create --print-join-command
      register: join_command_raw

    - name: set join command
      set_fact:
        join_command: "{{ join_command_raw.stdout_lines[0] }}"

- hosts: tag_role_kube_workernode
  become: yes
  tasks:
#    - name: get masternode ip
#      var:
#        masternode_ip: inventory_hostname
#      tags: tag_role_kube_masternode

    - name: join cluster
      shell: "{{ hostvars['10.200.20.60'].join_command }} >> node_joined.txt"
#      shell: "{{ inventory_hostname.join_command }} >> node_joined.txt"
      args:
        chdir: $HOME
        creates: node_joined.txt
#      tags: tag_role_kube_workernode
